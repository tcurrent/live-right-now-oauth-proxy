{
  "name": "live-right-now-oauth-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "tsc -p tsconfig.json",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240100.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}